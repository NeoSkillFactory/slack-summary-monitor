{
  "name": "slack-summary-monitor",
  "version": "1.0.0",
  "description": "Automatically monitors Slack channels for keywords and generates weekly summary reports with actionable insights.",
  "main": "scripts/slack-monitor.js",
  "scripts": {
    "start": "node scripts/slack-monitor.js run",
    "init-config": "node scripts/slack-monitor.js init",
    "report": "node scripts/report-generator.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --config jest.config.json"
  },
  "dependencies": {
    "@slack/web-api": "^7.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "keywords": ["slack", "monitoring", "reports", "automation"],
  "license": "MIT"
}
